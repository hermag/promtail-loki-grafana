resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: 750m
    memory: 750Mi
persistence:
  enabled: true
  type: pvc
  accessModes:
    - ReadWriteOnce
  existingClaim: loki-volume-claim-02
config:
  auth_enabled: false
